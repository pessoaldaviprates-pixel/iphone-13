/* =====================================================================
   CONFIGURAÇÃO DO NEON NEBULA
   Este é o único arquivo que você precisa editar.
   ===================================================================== */

window.NN_CONFIG = {

  // Endereço do Realtime Database (Firebase) que liga o modo online:
  // ranking de todos os jogadores, lista no painel ADM e presentes.
  // Deixando vazio (""), o jogo funciona 100% offline, sem conexão nenhuma.
  // Passo a passo e regras de segurança: veja o arquivo NUVEM.md.
  NUVEM_URL: "https://realtime-datase-default-rtdb.firebaseio.com"

};
